      // N = 9;  
      // double heap[] = {-1,99,80,85,17,30,84,2,16,1};  // size of array = N+1
      
      // display(heap);
      // sort(heap);
      // display(heap);
      // System.out.println(isSorted(heap));
